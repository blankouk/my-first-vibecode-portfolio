{
  "name": "artem-portfolio",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy:cf": "npm run build && wrangler pages deploy dist --project-name=artem-portfolio"
  },
  "dependencies": {
    "astro": "^6.3.1"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.0",
    "tailwindcss": "^4.3.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.90.0"
  },
  "overrides": {
    "vite": "^7.3.0"
  }
}
